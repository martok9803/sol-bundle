# 12 word mnemonic to derive private keys
MNEMONIC="twelve words here with spaces etc etc etc etc"

# Helius
RPC_URL="https://mainnet.helius-rpc.com/?api-key=YOUR_KEY"
WS_URL="wss://mainnet.helius-rpc.com/?api-key=YOUR_KEY"

# Jito
JITO_URL="ny.mainnet.block-engine.jito.wtf"
TIP_LAMPORTS=0        # set to 1000000 (0.001 SOL) if you want to tip

# Bot Defaults
NUM_SUB_WALLETS=20
SLIPPAGE_BPS=100
FEE_BUFFER_SOL=0.003
MIN_SPEND_SOL=0.0005

# Throttle: 1 on free RPC, 3–5 on paid
SEND_TXS_PER_SEC=1

# Route preference
USE_PUMPFUN_FIRST=1   # 1 = try Pump.fun first, else Jupiter

# Force skip Jito if you don't want to bundle
FORCE_RPC=0

# Drain all wallets to the external PublicAddress
EXTERNAL_WALLET=""
EXTERNAL_FEE_RESERVE_SOL=0.0005
